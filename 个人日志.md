
```
日期：2026.6.10
工作：项目交接   
```

```
日期：2026.6.11
工作：温习了信息熵、初步了解强化学习、读代码    
```

```
日期：2026.6.12
工作：理清并绘制了各代码脚本间的逻辑关系    
```

```
日期：2026.6.13
工作：尝试运行并熟悉现有代码    
```

```
日期：2026.6.14
工作：制作了节点控制面板，将大部分脚本接入面板，发现Node6(192.168.2.22)的流量恢复器用处不大，删除不影响正常通信；TLS通信RTT探测程序有问题，不能使用，目前的程序使用的是ICMP的通信时延，而分割数据包针对TLS，难以体现对通信时延的影响        
后续计划：深入学习PPO.py
```

```
日期：2026.6.16
工作：重构项目：修复了网络负载控制器无法修改负载占比的bug；发现了ICMP时延探测器因WiFi链路导致的不稳定性，利用四分位距法来进行了修复，效果显著。
```

```
日期：2026.6.17
工作：重构项目：写了个模拟TCP通信脚本，同时能够提供TCP的RTT时延；重写了信息熵探针，设计将归一化操作放到顶层，底层各司其职，后续方便迭代。
后续计划：研究探讨源代码中归一化的合理性，其中的硬编码“各设备信息熵最大值”的由来。
```

```
日期：2026.6.18
工作：研究数据包分割机制，在探索单节点TCP数据包分割方案时发现数据包分割后必须按顺序到达，单节点分割方案的可行性被排除，必须在靠近终端的一侧或终端本身设置乱序队列；将模拟TCP通信脚本完善成TLS1.2通信模式
```

```
日期：2026.6.22
工作：开始深入研究源代码中的PPO模型训练过程，通过chatgpt辅助理解发现其存在GAE计算和Return计算不正确的问题，导致 Critic 学习目标严重失真。（具体原理因相关知识储备较少，暂时未能理解）；正在重构着控制层的AdaptiveAgent.py（暂未完成但已有完整思路），该层将负责收集环境层的各种原始信息，并在此层进行归一化，并安排训练任务。
后续：控制层跟学习层相对较为复杂，所需理解时间可能较长，重构后的首次训练测试预计为6月24日或6月25日
```