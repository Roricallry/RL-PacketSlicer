设计一个能够根据当前网络状况，自主选择最佳网络分割方案(p,n)的程序。

## 1. 现状

state=(P, Latency_ICMP_median, Latency_TLS_avg)
训练方式：利用率\[60,70,80,90,95\]循环，每个利用率持续3小时

之前提到的“每次运行都会产生差距较大的不同结果”的问题，难以完全杜绝此类问题，目前通过
（1）修改cost= float(latency_TLS_avg > 100ms)，相较于此前的cost = (latency_TLS_avg - 100ms) ^ power，该cost惩罚函数降低了“低U的偶发高latency_TLS”对模型的影响，使得网络情况良好的情况下能够尽可能的拔高分割概率p。由于$cost\in[0,1]$，所以 惩罚系数$\lambda$ 需要设置的较大。
（2）不用 replay buffer 里的旧 action 的 log probability 来更新 alpha

### 2. 现存问题
表象：

